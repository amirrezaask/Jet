name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install
      - run: pnpm -r typecheck

  test-tauri:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-24.04]
    runs-on: ${{ matrix.os }}
    needs: typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/gharargah/src-tauri
      - name: Install Linux WebKit dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xvfb
      - run: pnpm install
      - run: npm install -g typescript-language-server typescript
      - run: cargo test --manifest-path apps/gharargah/src-tauri/Cargo.toml
      - name: Verify Tauri startup graph (no Shiki/diffs/xterm preload)
        run: pnpm --filter gharargah build
      - name: Tauri native suite (macOS)
        if: runner.os == 'macOS'
        run: pnpm test:tauri
        env:
          GHARARGAH_E2E_RUN_FLAKY: "1"
      - name: Tauri native suite (Linux/Xvfb)
        if: runner.os == 'Linux'
        run: xvfb-run -a pnpm test:tauri
        env:
          GHARARGAH_E2E_RUN_FLAKY: "1"

  test-bench:
    runs-on: macos-latest
    needs: typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/gharargah/src-tauri
      - run: pnpm install
      - run: pnpm test:bench
        env:
          GHARARGAH_SHELL: tauri
      - run: pnpm --filter gharargah bench:startup:release
