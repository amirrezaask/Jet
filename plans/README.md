# Gharargah Tauri implementation plans

Generated by the `improve` skill on 2026-07-11 at commit `a52fab2`. Execute in the order below. Each executor must read its plan fully, honor its STOP conditions, and update the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Establish release-grade Tauri verification | P0 | L | — | DONE (partial STOP) |
| 002 | Remove Shiki from Tauri startup | P0 | M | 001 | DONE |
| 003 | Bound Tauri host CPU, memory, and lifetimes | P0 | L | 001 | DONE |
| 004 | Make premium motion correct and frame-budgeted | P1 | L | 001, 002 | DONE |
| 005 | Close parity gaps before retiring Electron | P0 | L | 001, 003, 004 | DONE (user override: Electron removed) |

## Status notes (2026-07-12)

### 001 — DONE with intentional STOP
- CI: `test-tauri` job (macOS + Linux/Xvfb) + `cargo test` + frontend `gharargah build` startup-graph guard.
- Shared Electron specs run via `tauri-e2e`; critical non-agent skips restored; visual-evidence spec added.
- Custom `tests/tauri/webdriver.cjs` kept as thin adapter (W3C actions for pointer/keys). Full `@wdio/tauri-service` rewrite deferred — STOP: embedded driver already covers needed capabilities via existing Playwright↔WDIO bridge.
- Documented native-driver limitations: OS window drag, dnd-kit pointer capture (headed/manual).
- Agents E2E remain excluded (user directive).

### 002 — DONE
- `onlyExplicitManualChunks` + separate `shiki`/`diffs`/`xterm` chunks; preload helper no longer lives in Shiki.
- `scripts/verify-startup-graph.mjs` wired into `gharargah` build (and CI).
- `scripts/startup-bench.mjs` enforces `scripts/startup-budgets.json`.
- **Before/after (release, 5 samples)**:

  | Metric | Audit (`a52fab2`) | After Shiki defer (2026-07-12) |
  |---|---|---|
  | Cold host | 595 ms | 589 ms |
  | Warm host | 499–509 ms | 458–476 ms (median ≈473) |
  | Renderer | 271–275 ms | 250–285 ms |
  | Initial graph | Shiki 9.85 MB preloaded | only `preload-helper` |

- Aspirational warm≤300 / cold≤400 **not met**. Remaining blocker: `main-*.js` ≈2.17 MB gzip≈681 KB still on first paint + native webview/host init. Budgets file keeps aspirational numbers separately; enforced gates are regression guards (650/520/300).

### 003 — DONE
- Workspace activation cancels prior watchers; generation gated at publish boundaries; lifecycle unit test (20 reactivations).
- LSP bridge: blocking socket + 50 ms read/write timeouts (no 1 ms / 8 ms spin sleep).
- Terminal replay: chunk deque + bounded bytes; host-client incremental buffer sizing.

### 004 — DONE
- Hot glow rAF-batched; drop morph imperative (no per-frame React state); layout morph uses lightweight shell (no editor/terminal `cloneNode`).
- Universal caret teardown on portal/unmount; palette max-height / list clip tokens.
- Interaction budgets already in `tests/bench/budgets.json` (palette 50 ms, typing 16 ms).

### 005 — DONE (user override 2026-07-12)
- Electron app (`apps/jet-desktop`) + Node host (`packages/jet-host`) removed.
- Defaults: `pnpm dev` / `pnpm release` / CI → Tauri.
- Shared UI specs kept under `tests/electron/` and run via `tauri-e2e`.
- Remaining gaps (agents E2E, packaging polish) tracked outside retirement gate.

## Dependency notes

- Plan 001 comes first because the current Tauri suite has 6 failures, 13 skips, no CI job, and synthetic input gaps. Performance and parity changes need a trustworthy gate.
- Plan 002 follows the verification baseline and should land before motion work so startup/bundle changes are measured independently.
- Plan 003 fixes host lifecycle behavior that currently contaminates CPU, memory, reload, LSP, and terminal measurements.
- Plan 004 depends on the stable native-input test path from 001 and startup isolation from 002.
- Plan 005 Electron retirement completed by user override; Tauri is the sole shell.

## Audit baseline

- `pnpm -r typecheck`: passed.
- `cargo test -q` in `apps/gharargah/src-tauri`: 14 passed, with 4 warnings.
- `pnpm test:tauri`: 64 passed, 13 skipped, 6 failed in 9.3 minutes.
- Release startup, 5 samples: 595.3 ms cold; 498.7–508.8 ms warm; renderer 271–275 ms.
- Live process after editor + TypeScript LSP: Tauri shell 119,088 KiB RSS by `ps`; LSP processes approximately 237 MiB combined. Tauri sampled at 1.5–1.7% idle CPU and 41 threads.
- Production bundle: `shiki-*.js` 9.85 MB minified / 1.74 MB gzip and eagerly module-preloaded.

## Findings considered and rejected

- Replace React/CodeMirror with a native Rust renderer: rejected. It contradicts Gharargah's documented CodeMirror ownership model and would be a rewrite rather than a measured improvement.
- Add a GPU renderer now: rejected. Current bottlenecks are eager JavaScript, busy polling, lifecycle leaks, and DOM animation work; a renderer rewrite would not address those first-order costs.
- Remove Electron immediately after basic Tauri smoke passes: rejected. Native Tauri currently fails 6 shared E2E cases, excludes agents, has no Tauri CI, and lacks agent/extension parity.
- Micro-optimize ordinary Rust allocation before fixing lifecycle and polling: rejected. The measured and statically proven wins are higher-level: eliminate eager Shiki, 1 ms LSP polling, duplicate watchers, whole-DOM motion clones, and unbounded/repeated buffer scans.
